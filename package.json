{
  "name": "cypress-e2e",
  "version": "1.0.0",
  "description": "cypress end to end testing",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "cy:run": "yarn cypress run",
    "cypress:open:dev": "yarn cypress open --config-file cypress/cypress-config/cypress.dev.js",
    "cypress:open:hml": "yarn cypress open --config-file cypress/cypress-config/cypress.hml.js",
    "cypress:open:stg": "yarn cypress open --config-file cypress/cypress-config/cypress.stg.js",
    "cy:run-chrome": "cypress run --browser chrome",
    "cy:run-firefox": "cypress run --browser firefox"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "yarn": "^1.22.19"
  },
  "devDependencies": {
    "cypress": "4.11.0"
  }
}
